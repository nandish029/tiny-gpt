model:
  vocab_size: 89
  embedding_dim: 32
  max_context_length: 64
  num_heads: 4
  feed_forward_dim: 128
  num_layers: 2
training:
  batch_size: 16
  learning_rate: 3.0e-4
  train_steps: 5000
  val_interval: 100
  seed: 42
